<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
			 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
			 x:Class="ssLprojectFS.PersonDetailsPage"
			 xmlns:ViewModel="clr-namespace:ssLprojectFS;assembly=ssLprojectFS"
			 Title="Person's Details"
			 Padding="10">
	
	<ContentPage.Content>
		<StackLayout HorizontalOptions="Center"
					 Padding="20">
			<Grid>
				<Grid.RowDefinitions>
					<RowDefinition Height="*"/>
					<RowDefinition Height="*"/>
				</Grid.RowDefinitions>
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="20" />
					<ColumnDefinition Width="*" />
				</Grid.ColumnDefinitions>

				<Label Text="{Binding PersonDetails.Id}" Grid.Row="0" Grid.Column="0" />
				<Label Text="{Binding PersonDetails.Name}" Grid.Row="0" Grid.Column="1" />
				<Label Text="{Binding PersonDetails.Age}" Grid.Row="1" Grid.Column="0" />
				<Label Text="{Binding PersonDetails.Description}" Grid.Row="1" Grid.Column="1"/>
			</Grid>
			
			<Button Text="Back"
					Command="{Binding BackCommand}" />
		</StackLayout>
	</ContentPage.Content>
</ContentPage>
